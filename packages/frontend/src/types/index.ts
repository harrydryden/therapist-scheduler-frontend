/**
 * Re-export all shared API types from @therapist-scheduler/shared.
 * This file exists so that existing import paths (e.g. '../types') continue to work
 * without changing every component file.
 */
export * from '@therapist-scheduler/shared';
